import React, { FC } from "react";
import "./index.scss";

const Header: FC = () => {
  return (
    <header className="header">
      <span>i am header</span>
    </header>
  );
};

export default Header;
